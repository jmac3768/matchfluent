# Astro exposes env vars via import.meta.env.
# Client-visible vars MUST use the PUBLIC_ prefix (this key is used by the
# Quiz island in the browser, so it needs the prefix).
PUBLIC_EMAIL_API_KEY=your_email_provider_api_key_here
